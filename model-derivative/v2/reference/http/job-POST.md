---
title: "POST Create Translation Job"
url_path: reference/http//job-POST
product: "Model Derivative API"
surface: "model-derivative-v2"
protocol: "REST"
document_kind: "endpoint"
api_version: "v2"
section: "reference"
category: "http"
operation_id: "job-POST"
method: "POST"
path: "/modelderivative/v2/designdata/job"
auth_context: "user context optional"
scopes: ["data:read","data:write"]
verification: "docs-only"
---
# modelderivative/v2/designdata/job

Creates a job to translate the specified source design to another format, generating derivatives of the source design. You can optionally:
- Extract selected parts of a design and export the set of geometries in OBJ format.
- Generate different-sized thumbnails.

When the translation job runs, details about the process and generated derivatives are logged to a JSON file known as a manifest. This manifest is typically created during the first translation of a source design. Subsequently, the system updates the same manifest whenever a translation is performed for that design.

If necessary, you can set the `x-ads-force` header to `true`. The system will then delete the existing manifest and create a new one. However, be aware that doing so will also delete all previously generated derivatives.

## Resource Information

| Method and URI | POST https://developer.api.autodesk.com/modelderivative/v2/designdata/job |
| --- | --- |
| Authentication Context | User context optional |
| Required OAuth Scopes | `data:read` `data:write` |
| Data Format | JSON |

### Request

## Headers

| Authorization*string | Must be `Bearer <token>`, where `<token>` is a two-legged access token obtained via a [Client Credentials Grant flow](https://aps.autodesk.com/en/docs/oauth/v2/tutorials/get-2-legged-token/), or a three-legged access token obtained via an [Authorization Code flow](https://aps.autodesk.com/en/docs/oauth/v2/tutorials/get-3-legged-token/) or a [Secure Service Account (SSA) flow](https://aps.autodesk.com/en/docs/ssa/v1/tutorials/getting-started-with-ssa/task3-generate-3-legged-access-token/).
The SSA flow is designed for headless server-to-server operations. While it functions like a two-legged flow (no user interaction), it is classified as three-legged because it preserves user context. |
| --- | --- |
| x-ads-forcestring | Specifies how the translation job must handle any existing manifest and derivatives.

`true` - Deletes the existing manifest and derivatives of the source design before translation.
`false` - (Default) Updates existing manifest and generates derivatives only for the formats that the source design has no derivatives. |
| x-ads-derivative-formatstring | Specifies which Object IDs to return if the design has legacy SVF derivatives generated by the BIM Docs or Forma Data Management service. Possible values:

`latest` - (Default) Return SVF2 Object IDs.
`fallback` - Return SVF Object IDs.

**Note:**

This header applies only to designs with legacy SVF derivatives generated by the BIM 360 Docs or Forma Data Management service.
The BIM 360 Docs or Forma Data Management service now generates SVF2 derivatives. SVF2 Object IDs may not be compatible with the SVF Object IDs previously generated by the BIM 360 Docs or Forma Data Management service. Setting this header to `fallback` may resolve backward compatibility issues resulting from Object IDs of legacy SVF derivatives being retained on the client side.
If you use this header with one derivative (URN), you must use it consistently across the following operations:

[Create Translation Job](https://aps.autodesk.com/en/docs/model-derivative/v2/reference/http/job-POST) (for OBJ output)
[Fetch Object Tree](https://aps.autodesk.com/en/docs/model-derivative/v2/reference/http/urn-metadata-modelguid-GET)
[Fetch All Properties](https://aps.autodesk.com/en/docs/model-derivative/v2/reference/http/urn-metadata-guid-properties-GET)
[Fetch Specific Properties](https://aps.autodesk.com/en/docs/model-derivative/v2/reference/http/metadata/urn-metadata-guid-properties-query-POST) |
| regionstring | Specifies the data center where the manifest and derivatives of the specified source design are stored. Possible values are:

`US` - (Default) Data center for the US region.
`EMEA` - Data center for the European Union, Middle East, and Africa.
`AUS` - Data center for the Australia region.
`CAN` - Data center for the Canada region.
`DEU` - Data center for the Germany region.
`IND` - Data center for the India region.
`JPN` - Data center for the Japan region.
`GBR` - Data center for the United Kingdom region. |
| Content-Type*string | Must be `application/json` |

### Request

## Body Structure

| input*object | Describes the attributes of the source design. |
| --- | --- |
| urn*string | The URL safe Base64 encoded URN of the source design.
**Note:** The URN is returned as the `objectID` once you complete uploading the source design to APS. This value must be converted to a [Base64 (URL Safe) encoded](http://www.freeformatter.com/base64-encoder.html) string before you can specify it for this attribute. |
| compressedUrnboolean | `true` - The source design is compressed as a zip file. If set to `true`, you need to define the `rootFilename` attribute.
`false` - (Default) The source design is not compressed. |
| rootFilenamestring | The file name of the top-level component of the source design. Mandatory if `compressedUrn` is set to `true`. |
| checkReferencesboolean | `true` - Instructs the system to check for references and, if any exist, download all referenced files. Setting this attribute to `true` is mandatory when translating source designs consisting of multiple files, such as Autodesk Inventor assemblies.
`false` - (Default) Instructs the system not to check for references. |
| output*object | Describes the attributes of the requested derivatives. |
| destinationobject | Deprecated Specifies where to store generated derivatives.
**Note:** Please use the `region` header instead of this attribute. |
| regionstring | Deprecated Specifies where to store generated derivatives. Possible values are:

`US`: (Default) Store derivatives at a data center for the United States of America.
`EMEA`: Store derivatives at a data center for the European Union.
`AUS`: Store derivatives at a data center for the Australia region.
`CAN` - Data center for the Canada region.
`DEU` - Data center for the Germany region.
`IND` - Data center for the India region.
`JPN` - Data center for the Japan region.
`GBR` - Data center for the United Kingdom region.

**Note**: Please use the `region` header instead of this attribute. |
| formats*array: object | An array of objects, where each object represents a requested derivative format. You can request multiple derivatives. |
| anyOf | Contains the configuration settings for each output type a translation job must produce. The available options depend on the output type. |
| SVF Optionsobject | Describes the options for translation jobs producing SVF outputs. |
| viewsarray: string | Specifies the type of view to generate. Possible values are `2d` and `3d`. |
| typestring | The requested output type. Must be `svf` in this case. |
| advancedone of | Contains advanced configuration settings for translation jobs producing `svf` outputs. The available options depend on the input type. |
| Revit Optionsobject | Contains advanced configuration settings for translation jobs processing Revit inputs. |
| 2dviewsenum:string | The format that 2D views must be rendered to. Possible values:

`legacy` - (Default) Render to a model derivative specific file format.
`pdf` - Render to the PDF file format. When the source design is of type Revit, applies only to Revit 2022 files and newer. If the source design is of type DWG, only properties with semantic values are extracted. |
| extractorVersionenum:string | Specifies what version of the Revit translator/extractor to use. Applicable only when the source design is of type RVT. Possible values:

`next` - Makes the translation job use the newest available version of the translator/extractor. This option is meant to be used by beta testers who wish to test a pre-release version of the translator. If no pre-release version is available, the system uses the current official release version.
`previous` - Makes the translation job use the previous official release version of the translator/extractor. This option is meant to be used as a workaround in case of regressions caused by a new official release of the translator/extractor. If this attribute is not specified, the system uses the current official release version. |
| generateMasterViewsboolean | Specifies whether to generate master views when translating from the Revit input format to SVF. This option is ignored for all other input formats. Possible values:

`true` - Generate master views.
`false` - (Default) Do not generate master views.

Master views are 3D views that are generated for each phase of the Revit model. A master view contains all elements (including room elements) present in the host model for that phase. The display name of a master view defaults to the name of the phase it is generated from. However, if a view with that name already exists, the Model Derivative service appends a suffix to the default display name.
**Notes:**

Master views do not contain elements from linked models.
Enabling this option can increase the time it takes to translate the model. |
| materialModeenum:string | Specifies the materials to apply to the generated SVF derivatives. Applicable only when the source design is of type RVT. Possible values:

`auto` - (Default) Use the current setting of the default view of the input file.
`basic` - Use basic materials.
`autodesk` - Use Autodesk materials. |
| DGN Optionsobject | Contains advanced configuration settings for translation jobs processing DGN inputs. |
| requestedLinkageIDsarray: integer | An array containing user data linkage IDs of the linkage data to be extracted from the DGN file. Linkage data is not extracted if you do not specify this attribute.
**Note:** See the `Linkage_<ID>` property naming convention in the [Field Guide](https://aps.autodesk.com/en/docs/model-derivative/v2/overview/field-guide) to understand how to identify the linkage data. |
| DWG Optionsobject | Contains advanced configuration settings for translation jobs processing DWG inputs. |
| 2dviewsenum:string | The format that 2D views must be rendered to. Possible values:

`legacy` - (Default) Render to a model derivative specific file format.
`pdf` - Render to the PDF file format. When the source design is of type Revit, applies only to Revit 2022 files and newer. If the source design is of type DWG, only properties with semantic values are extracted. |
| IDW Optionsobject | Contains advanced configuration settings for translation jobs processing IDW inputs. |
| 2dviewsenum:string | The format that 2D views must be rendered to. Possible values:

`legacy` - (Default) Render to a model derivative specific file format.
`pdf` - Render to the PDF file format. When the source design is of type Revit, applies only to Revit 2022 files and newer. If the source design is of type DWG, only properties with semantic values are extracted. |
| IFC Optionsobject | Contains advanced configuration settings for translation jobs processing IFC inputs. See [IFC Translations](https://aps.autodesk.com/en/docs/model-derivative/v2/developers_guide/supported-translations/ifc-file-translations/) for more information. |
| conversionMethodenum:string | Specifies which IFC loader to use during translation. Applicable only when the source design is of type IFC. Possible values:

`legacy` - (Default) Use IFC loader version 1, which is based on the Navisworks IFC loader.
`modern` - Use IFC loader version 2, which is based on the Revit IFC loader.
`v3` - Use IFC loader version 3, which is based on the Revit IFC loader.
`v4` - **(Recommended)** Use IFC loader version 4, which is a native solution specifically designed for Autodesk Platform Services (APS). It does not depend on Navisworks or Revit. |
| buildingStoreysenum:string | Specifies how storeys are translated. Applicable only when the source design is of type IFC. Possible values:

`hide` - (Default) Storeys are translated but not visible by default.
`show` - Storeys are translated and are visible by default.
`skip` - Storeys are not translated.

**Note:** These options are not applicable when `conversionMethod` is set to `legacy`. |
| spacesenum:string | Specifies how spaces are translated. Applicable only when the source design is of type IFC. Possible values:

`hide` - (Default) Spaces are translated but not visible by default.
`show` - Spaces are translated and are visible by default.
`skip` - Spaces are not translated.

**Note:** These options are not applicable when `conversionMethod` is set to `legacy`. |
| openingElementsenum:string | Specifies how openings are translated. Applicable only when the source design is of type IFC. Possible values:

`hide` - (Default) Openings are translated but are not visible by default.
`show` - Openings are translated and are visible by default.
`skip` - Openings are not translated.

**Note:** These options are not applicable when `conversionMethod` is set to `legacy`. |
| NWD Optionsobject | Contains advanced configuration settings for translation jobs processing NWD inputs. |
| hiddenObjectsboolean | Specifies whether hidden objects must be extracted. Applicable only when the source design type is Navisworks.

`true` - Extract hidden objects from the input file.
`false` - (Default) Do not extract hidden objects from the input file. |
| basicMaterialPropertiesboolean | Specifies whether basic material properties must be extracted. Applicable only when the source design type is Navisworks.

`true` - Extract properties for basic materials.
`false` - (Default) Do not extract properties for basic materials. |
| autodeskMaterialPropertiesboolean | Specifies whether Autodesk material properties must be extracted. Applicable only when the source design type is Navisworks.

`true` - Extract properties for Autodesk materials.
`false` - (Default) Do not extract properties for Autodesk materials. |
| timelinerPropertiesboolean | Specifies whether timeliner properties must be extracted. Applicable only when the source design type is Navisworks.

`true` - Extract timeliner properties.
`false` - (Default) Do not extract timeliner properties. |
| VUE Optionsobject | Contains advanced configuration settings for translation jobs processing VUE inputs. |
| hierarchyenum:string | Specifies how the hierarchy of items are determined. Applicable only when the source design is of type VUE. Possible values:

`Classic` - (Default) Uses hardcoded rules to set the hierarchy of items.
`SystemPath` - Uses a linked XML or MDB2 properties file to set hierarchy of items. You can use this option to make the organization of items consistent with SmartPlant 3D.

**Notes:**

The functioning of the `SystemPath` depends on the default setting of the property `SP3D_SystemPath` or `System Path`.
The pathing delimiter must be `\`.
If you want to customize further, import the VUE file to Navisworks. After that, use POST job on the resulting Navisworks (nwc/nwd) file. |
| SVF2 Optionsobject | Describes the options for translation jobs producing SVF2 outputs. |
| viewsarray: string | Specifies the type of view to generate. Possible values are `2d` and `3d`. |
| typestring | The requested output type. Must be `svf2` in this case. |
| advanced | Contains advanced configuration settings for translation jobs producing `svf2` outputs. The available options depend on the input type. |
| anyOf | Contains advanced configuration settings for translation jobs producing `svf2` outputs. The available options depend on the input type. |
| Revit Optionsobject | Contains advanced configuration settings for translation jobs processing Revit inputs. |
| 2dviewsenum:string | The format that 2D views must be rendered to. Possible values:

`legacy` - (Default) Render to a model derivative specific file format.
`pdf` - Render to the PDF file format. When the source design is of type Revit, applies only to Revit 2022 files and newer. If the source design is of type DWG, only properties with semantic values are extracted. |
| extractorVersionenum:string | Specifies what version of the Revit translator/extractor to use. Applicable only when the source design is of type RVT. Possible values:

`next` - Makes the translation job use the newest available version of the translator/extractor. This option is meant to be used by beta testers who wish to test a pre-release version of the translator. If no pre-release version is available, the system uses the current official release version.
`previous` - Makes the translation job use the previous official release version of the translator/extractor. This option is meant to be used as a workaround in case of regressions caused by a new official release of the translator/extractor. If this attribute is not specified, the system uses the current official release version. |
| generateMasterViewsboolean | Specifies whether to generate master views when translating from the Revit input format to SVF. This option is ignored for all other input formats. Possible values:

`true` - Generate master views.
`false` - (Default) Do not generate master views.

Master views are 3D views that are generated for each phase of the Revit model. A master view contains all elements (including room elements) present in the host model for that phase. The display name of a master view defaults to the name of the phase it is generated from. However, if a view with that name already exists, the Model Derivative service appends a suffix to the default display name.
**Notes:**

Master views do not contain elements from linked models.
Enabling this option can increase the time it takes to translate the model. |
| materialModeenum:string | Specifies the materials to apply to the generated SVF derivatives. Applicable only when the source design is of type RVT. Possible values:

`auto` - (Default) Use the current setting of the default view of the input file.
`basic` - Use basic materials.
`autodesk` - Use Autodesk materials. |
| DGNobject | Contains advanced configuration settings for translation jobs processing DGN inputs. |
| requestedLinkageIDsarray: integer | An array containing user data linkage IDs of the linkage data to be extracted from the DGN file. Linkage data is not extracted if you do not specify this attribute. |
| DWG Optionsobject | Contains advanced configuration settings for translation jobs processing DWG inputs. |
| 2dviewsenum:string | The format that 2D views must be rendered to. Possible values:

`legacy` - (Default) Render to a model derivative specific file format.
`pdf` - Render to the PDF file format. When the source design is of type Revit, applies only to Revit 2022 files and newer. If the source design is of type DWG, only properties with semantic values are extracted. |
| IDW Optionsobject | Contains advanced configuration settings for translation jobs processing IDW inputs. |
| 2dviewsenum:string | The format that 2D views must be rendered to. Possible values:

`legacy` - (Default) Render to a model derivative specific file format.
`pdf` - Render to the PDF file format. When the source design is of type Revit, applies only to Revit 2022 files and newer. If the source design is of type DWG, only properties with semantic values are extracted. |
| IFC Optionsobject | Contains advanced configuration settings for translation jobs processing IFC inputs. See [IFC Translations](https://aps.autodesk.com/en/docs/model-derivative/v2/developers_guide/supported-translations/ifc-file-translations/) for more information. |
| conversionMethodenum:string | Specifies which IFC loader to use during translation. Applicable only when the source design is of type IFC. Possible values:

`legacy` - (Default) Use IFC loader version 1, which is based on the Navisworks IFC loader.
`modern` - Use IFC loader version 2, which is based on the Revit IFC loader.
`v3` - Use IFC loader version 3, which is based on the Revit IFC loader.
`v4` - **(Recommended)** Use IFC loader version 4, which is a native solution specifically designed for Autodesk Platform Services (APS). It does not depend on Navisworks or Revit. |
| buildingStoreysenum:string | Specifies how storeys are translated. Applicable only when the source design is of type IFC. Possible values:

`hide` - (Default) Storeys are translated but not visible by default.
`show` - Storeys are translated and are visible by default.
`skip` - Storeys are not translated.

**Note:** These options are not applicable when `conversionMethod` is set to `legacy`. |
| spacesenum:string | Specifies how spaces are translated. Applicable only when the source design is of type IFC. Possible values:

`hide` - (Default) Spaces are translated but not visible by default.
`show` - Spaces are translated and are visible by default.
`skip` - Spaces are not translated.

**Note:** These options are not applicable when `conversionMethod` is set to `legacy`. |
| openingElementsenum:string | Specifies how openings are translated. Applicable only when the source design is of type IFC. Possible values:

`hide` - (Default) Openings are translated but are not visible by default.
`show` - Openings are translated and are visible by default.
`skip` - Openings are not translated.

**Note:** These options are not applicable when `conversionMethod` is set to `legacy`. |
| NWD Optionsobject | Contains advanced configuration settings for translation jobs processing NWD inputs. |
| hiddenObjectsboolean | Specifies whether hidden objects must be extracted. Applicable only when the source design type is Navisworks.

`true` - Extract hidden objects from the input file.
`false` - (Default) Do not extract hidden objects from the input file. |
| basicMaterialPropertiesboolean | Specifies whether basic material properties must be extracted. Applicable only when the source design type is Navisworks.

`true` - Extract properties for basic materials.
`false` - (Default) Do not extract properties for basic materials. |
| autodeskMaterialPropertiesboolean | Specifies whether Autodesk material properties must be extracted. Applicable only when the source design type is Navisworks.

`true` - Extract properties for Autodesk materials.
`false` - (Default) Do not extract properties for Autodesk materials. |
| timelinerPropertiesboolean | Specifies whether timeliner properties must be extracted. Applicable only when the source design type is Navisworks.

`true` - Extract timeliner properties.
`false` - (Default) Do not extract timeliner properties. |
| VUE Optionsobject | Contains advanced configuration settings for translation jobs processing VUE inputs. |
| hierarchyenum:string | Specifies how the hierarchy of items are determined. Applicable only when the source design is of type VUE. Possible values:

`Classic` - (Default) Uses hardcoded rules to set the hierarchy of items.
`SystemPath` - Uses a linked XML or MDB2 properties file to set hierarchy of items. You can use this option to make the organization of items consistent with SmartPlant 3D.

**Notes:**

The functioning of the `SystemPath` depends on the default setting of the property `SP3D_SystemPath` or `System Path`.
The pathing delimiter must be `\`.
If you want to customize further, import the VUE file to Navisworks. After that, use POST job on the resulting Navisworks (nwc/nwd) file. |
| Thumbnail Optionsobject | Describes the options for translation jobs producing thumbnail outputs. |
| typestring | The requested output type. Must be `thumbnail` in this case. |
| advancedobject | Contains advanced configuration settings for translation jobs producing thumbnail outputs. |
| widthint | Width of thumbnail in pixels. Possible values:

`100` - 100 pixels wide.
`200` - 200 pixels wide.
`400` - 400 pixels wide.

If `width` is omitted, but `height` is specified, `width` defaults to `height`. If both `width` and `height` are omitted, the server will return a thumbnail closest to `200`, if such a thumbnail is available. |
| heightint | Height of thumbnails in pixels. Possible values:

`100` - 100 pixels high.
`200` - 200 pixels high.
`400` - 400 pixels high.

If `height` is omitted, but `width` is specified, `height` defaults to `width`. If both `width` and `height` are omitted, the server will return a thumbnail closest to `200`, if such a thumbnail is available. |
| STL Optionsobject | Describes the options for translation jobs producing STL outputs. |
| typestring | The requested output type. Must be `stl` in this case. |
| advancedobject | Contains advanced configuration settings for translation jobs producing `stl` outputs. |
| formatenum:string | Specifies the format of the file to create when the specified output is STL. Possible values:

`ascii` - Create derivative as an ASCII STL file.
`binary` - (Default) Create derivative as a binary STL file. |
| exportColorboolean | `true` - (Default) Exports color.
`false` - Does not export color. |
| exportFileStructureenum:string | Specifies the structure of the derivative when the specified output is STL. Possible values:

`single` - (Default) Create one STL file for all the input files (assembly file).
`multiple` - Create a separate STL file for each object. |
| STEP Optionsobject | Describes the options for translation jobs producing STEP outputs. |
| typestring | The requested output type. Must be `step` in this case. |
| advancedobject | Contains advanced configuration settings for translation jobs producing a STEP output. |
| applicationProtocolenum:string | Specifies the application protocol to use when the output type is STEP. Possible values:

`203` - Configuration controlled design.
`214` - (Default) Core data for automotive mechanical design processes.
`242` - Managed model based 3D engineering. |
| tolerancenumber | Possible values are between `0` and `1`. By default it is set at 0.001. |
| IGES Optionsobject | Describes the options for translation jobs producing IGES outputs. |
| typestring | The requested output type. Must be `iges` in this case. |
| advancedobject | Contains advanced configuration settings for translation jobs producing IGES outputs. |
| tolerancenumber | Possible values are between `0` and `1`. By default it is set at 0.001. |
| surfaceTypeenum:string | Specifies the surface type to export as when the output is IGES. Possible values:

`bounded` - (Default) Bounded surface.
`trimmed` - Trimmed surface.
`wireframe` - Wireframe surface. |
| sheetTypeenum:string | The sheet body type to export as when the output is IGES. Possible values:

`open` - Open sheet body.
`shell` - Shell sheet body.
`surface` - (Default) Surface sheet body.
`wireframe` - Wireframe sheet body. |
| solidTypeenum:string | The solid body type to export as when the output is IGES. Possible values:

`solid` - (Default) Solid body.
`surface` - Surface body.
`wireframe` - Wireframe body. |
| OBJ Optionsobject | Describes the options for translation jobs producing OBJ outputs. |
| typestring | The requested output type. Must be `obj` in this case. |
| advancedobject | Contains advanced configuration settings for translation jobs producing `obj` outputs. |
| exportFileStructureenum:string | Specifies the structure of the derivative when the specified output is STL. Possible values:

`single` - (Default) Create one STL file for all the input files (assembly file).
`multiple` - Create a separate STL file for each object. |
| unitenum:string | The units the models must be translated to, when the output type is OBJ. For example, from millimeters (10, 123, 31) to centimeters (1.0, 12.3, 3.1). If the source unit or the unit you are translating to is not supported, the values remain unchanged. Possible values:

`meter`
`decimeter`
`centimeter`
`millimeter`
`micrometer`
`nanometer`
`yard`
`foot`
`inch`
`mil`
`microinch`

**Note:** Not supported when the source design is F3D. |
| modelGuidstring | Required for geometry extractions. Specifies the ID of the Model View that contains the geometry to extract. |
| objectIdsarray: integer | Required for geometry extractions. Specifies the IDs of the objects (`objectID`) to extract. -1 will extract the entire model. |
| DWG Optionsobject | Describes the options for translation jobs producing DWG outputs. |
| typestring | The requested output type. Must be `dwg` in this case. |
| advancedobject | Contains advanced configuration settings for translation jobs producing DWG outputs. |
| exportSettingNamestring | The export settings should be one of the DWG Export settings pre-saved in the source design. |
| IFC Optionsobject | Describes the options for translation jobs producing IFC outputs. |
| typestring | The requested output type. Must be `ifc` in this case. |
| advancedobject | Contains advanced configuration settings for translation jobs producing IFC outputs. |
| exportSettingNamestring | The export settings should be one of the IFC Export settings pre-saved in the source design. See [IFC Translations](https://aps.autodesk.com/en/docs/model-derivative/v2/developers_guide/supported-translations/ifc-file-translations/) for more information. |
| miscobject | Contains webhook configuration settings for notifying external systems about translation job events. |
| workflowstring | The workflow ID of the webhook that listens to Model Derivative events. It must be 36 characters or less and can only contain alphanumeric characters (A-Z, 0-9) and hyphens (-). |
| workflowAttributeobject | A user-defined JSON object, which you can use to set some custom workflow information. It must be less than 1KB and is ignored if `misc.workflow` is not specified. |

### Response

## HTTP Status Code Summary

| 200OK | The translation job was successfully created and started. |
| --- | --- |
| 201Created | The requested derivative already exists, so a new derivative will not be generated. |
| 400Bad Request | The server was unable to process the request. The syntax of the request is malformed or the request is missing a required header. Do not repeat the request without fixing the issue. The response body may indicate what is wrong with the request. |
| 401Unauthorized | The supplied authorization header was not valid or the supplied token scope was not acceptable. Verify authentication and try again. |
| 403Forbidden | The request was successfully validated but lacking the required permissions. Verify your credentials and permissions before you send this request again. |
| 404Not Found | The requested resource was not found. Review the request and try again. |
| 406Not Acceptable | The request contains values that are not acceptable. For example, the output type you requested may not be supported. Please check your request parameters and modify them to contain only supported values. |
| 409Conflict | The request could not be completed due to a conflict with the current state of a resource. For example, you may be trying to create a resource that is currently being created by another request. Please review the response body for details and adjust your request accordingly. |
| 429Too Many Requests | The request was rejected because too many requests were sent within a specific time period. Wait before retrying the request. Check the response headers for rate limit information and recommended retry timing. |
| 500Internal Server Error | An unexpected error occurred on the server, preventing it from completing your request. Please try again later. If the issue persists, contact the support team for assistance. |

### Response

## Header (200)

| x-ads-app-identifierstring | The service identifier. Comprises of the service name, version, and environment. |
| --- | --- |
| x-ads-startup-timestring | The service startup time, in the following date format: `EEE MMM dd HH:mm:ss Z yyyy`. |
| x-ads-durationstring | The amount of time spent servicing the request, in milliseconds. |

### Response

## Body Structure (200)

| resultstring | A message indicating the result of the translation job request. Returns ‘success’ when the job is accepted and processing begins. |
| --- | --- |
| urnstring | The URL-safe Base64 encoded URN of the source design that was submitted for translation. This value uniquely identifies the design file in the system. |
| acceptedJobsobject | List of the requested outputs. |
| outputobject | Identical to the `output` object of the request body. For information on each attribute, see the request body structure description. |

### Response

## Body Structure (201)

Response for 201 has no body.

The following examples return raw HTTP headers and JSON objects. For a more developer-friendly experience, consider using our [TypeScript SDK](https://aps.autodesk.com/en/docs/model-derivative/v2/reference/typescript-sdk/) or [.NET SDK](https://aps.autodesk.com/en/docs/model-derivative/v2/reference/dot-net-sdk). Both provide strongly typed data with IntelliSense support, offering code completion, error checking, and tooltips that reduce the need to reference JSON schemas.

## Example 1

A successful attempt kicking off a job for the translation of an Inventor part file to SVF2. The derivatives are to be stored in the EMEA region.

```
curl --location 'https://developer.api.autodesk.com/modelderivative/v2/designdata/job' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer eyJh...' \
--data '{
  "input": {
   "urn": "dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6anAtYnVja2V0LTI1MDUwMi9ib3guaXB0"
  },
  "output": {
   "destination": {
    "region": "EMEA"
   },
   "formats": [
    {
      "type": "svf2",
      "views": [
       "2d",
       "3d"
      ]
    }
   ]
  }
}'
```

```
// Define headers for the request
const myHeaders = new Headers();
myHeaders.append("Content-Type", "application/json"); // Specify the content type
myHeaders.append("Authorization", "Bearer eyJh..."); // Add authorization token

// Define the request payload
const raw = JSON.stringify({
  "input": {
   "urn": "dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6anAtYnVja2V0LTI1MDUwMi9ib3guaXB0" // Unique identifier for the file
  },
  "output": {
   "destination": {
    "region": "EMEA" // Specify the region for the output
   },
   "formats": [
    {
      "type": "svf2", // Output format
      "views": [
       "2d", // Include 2D view
       "3d"  // Include 3D view
      ]
    }
   ]
  }
});

// Configure the request options
const requestOptions = {
  method: "POST", // HTTP method
  headers: myHeaders, // Attach headers
  body: raw, // Attach the request payload
  redirect: "follow" // Follow redirects if any
};

// Send the request using the Fetch API
fetch("https://developer.api.autodesk.com/modelderivative/v2/designdata/job", requestOptions)
  .then((response) => response.text()) // Parse the response as text
  .then((result) => console.log(result)) // Log the result
  .catch((error) => console.error(error)); // Handle any errors
```

```
var client = new HttpClient();
var request = new HttpRequestMessage(HttpMethod.Post, "https://developer.api.autodesk.com/modelderivative/v2/designdata/job");
// Add required headers
request.Headers.Add("Authorization", "Bearer eyJh..."); // Authorization token for the API

// Define the request body
var content = new StringContent(
  "{\n" +
  "    \"input\": {\n" +
  "        \"urn\": \"dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6anAtYnVja2V0LTI1MDUwMi9ib3guaXB0\"\n" + // Unique identifier for the file
  "    },\n" +
  "    \"output\": {\n" +
  "        \"destination\": {\n" +
  "            \"region\": \"EMEA\"\n" + // Specify the region for the output
  "        },\n" +
  "        \"formats\": [\n" +
  "            {\n" +
  "                \"type\": \"svf2\",\n" + // Output format
  "                \"views\": [\n" +
  "                    \"2d\",\n" + // Include 2D view
  "                    \"3d\"\n" + // Include 3D view
  "                ]\n" +
  "            }\n" +
  "        ]\n" +
  "    }\n" +
  "}", null, "application/json"); // Specify content type as JSON

// Attach the content to the request
request.Content = content; // Add the request body to the HTTP request

// Send the request and await the response
var response = await client.SendAsync(request); // Send the POST request to the API

// Ensure the response indicates success
response.EnsureSuccessStatusCode(); // Throw an exception if the response status is not successful

// Output the response content
Console.WriteLine(await response.Content.ReadAsStringAsync()); // Print the response content to the console
```

```
import requests
import json

# Define the API endpoint URL
url = "https://developer.api.autodesk.com/modelderivative/v2/designdata/job"  # Endpoint for the Model Derivative API

# Define the request payload
payload = json.dumps({
  "input": {
   "urn": "dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6anAtYnVja2V0LTI1MDUwMi9ib3guaXB0"  # Unique identifier for the file to be translated
  },
  "output": {
   "destination": {
    "region": "EMEA"  # Specify the region where the output will be stored
   },
   "formats": [
    {
      "type": "svf2",  # Specify the output format as SVF2
      "views": [
       "2d",  # Include 2D view in the output
       "3d"   # Include 3D view in the output
      ]
    }
   ]
  }
})

# Define the request headers
headers = {
  'Content-Type': 'application/json',  # Specify the content type as JSON
  'Authorization': 'Bearer eyJh...',   # Add the authorization token for API access
}

# Send the POST request to the API
response = requests.request("POST", url, headers=headers, data=payload)  # Make the API call with the defined payload and headers

# Print the response from the API
print(response.text)  # Output the API response to the console
```

### Response

```
HTTP/1.1 200 OK
Server: nginx
Date: Fri, 02 May 2025 10:27:50 GMT
Content-Type: application/json; charset=utf-8
Content-Length: 205
Connection: keep-alive
x-ads-duration: 2717 ms
x-ads-startup-time: Thu Apr 24 11:14:32 UTC 2025
x-ads-app-identifier: platform-viewing-2025.02.01.1205.2f1fdcae1-staging
strict-transport-security: max-age=31536000; includeSubDomains; preload
x-frame-options: SAMEORIGIN
x-request-id: b2afa5ab-b63f-49f5-8c3a-697ad456b99f

{
  "result": "success",
  "urn": "dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6anAtYnVja2V0LTI1MDUwMi9ib3guaXB0",
  "acceptedJobs": {
    "output": {
      "destination": {
        "region": "EMEA"
      },
      "formats": [
        {
          "type": "svf2",
          "views": [
            "2d",
            "3d"
          ]
        }
      ]
    }
  }
}
```

## Example 2

This example demonstrates the behavior when you initiate a job for a source file that has already been translated. Although the request is identical as Example 1, you’ll notice a different response. This is because this file was previously processed during the execution of Example 1.

### Request

```
curl --location 'https://developer.api.autodesk.com/modelderivative/v2/designdata/job' \
  --header 'Content-Type: application/json' \
  --header 'Authorization: Bearer eyJh...' \
  --data '{
   "input": {
    "urn": "dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6anAtYnVja2V0LTI1MDUwMi9ib3guaXB0"
   },
   "output": {
    "destination": {
      "region": "EMEA"
    },
    "formats": [
      {
       "type": "svf2",
       "views": [
        "2d",
        "3d"
       ]
      }
    ]
   }
  }'
```

```
// Define headers for the request
const myHeaders = new Headers();
myHeaders.append("Content-Type", "application/json"); // Specify the content type
myHeaders.append("Authorization", "Bearer eyJh..."); // Add authorization token

// Define the request payload
const raw = JSON.stringify({
  "input": {
   "urn": "dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6anAtYnVja2V0LTI1MDUwMi9ib3guaXB0" // Unique identifier for the file
  },
  "output": {
   "destination": {
    "region": "EMEA" // Specify the region for the output
   },
   "formats": [
    {
      "type": "svf2", // Output format
      "views": [
       "2d", // Include 2D view
       "3d"  // Include 3D view
      ]
    }
   ]
  }
});

// Configure the request options
const requestOptions = {
  method: "POST", // HTTP method
  headers: myHeaders, // Attach headers
  body: raw, // Attach the request payload
  redirect: "follow" // Follow redirects if any
};

// Send the request using the Fetch API
fetch("https://developer.api.autodesk.com/modelderivative/v2/designdata/job", requestOptions)
  .then((response) => response.text()) // Parse the response as text
  .then((result) => console.log(result)) // Log the result
  .catch((error) => console.error(error)); // Handle any errors
```

```
var client = new HttpClient();
var request = new HttpRequestMessage(HttpMethod.Post, "https://developer.api.autodesk.com/modelderivative/v2/designdata/job");
// Add required headers
request.Headers.Add("Authorization", "Bearer eyJh..."); // Authorization token for the API

// Define the request body
var content = new StringContent(
  "{\n" +
  "    \"input\": {\n" +
  "        \"urn\": \"dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6anAtYnVja2V0LTI1MDUwMi9ib3guaXB0\"\n" + // Unique identifier for the file
  "    },\n" +
  "    \"output\": {\n" +
  "        \"destination\": {\n" +
  "            \"region\": \"EMEA\"\n" + // Specify the region for the output
  "        },\n" +
  "        \"formats\": [\n" +
  "            {\n" +
  "                \"type\": \"svf2\",\n" + // Output format
  "                \"views\": [\n" +
  "                    \"2d\",\n" + // Include 2D view
  "                    \"3d\"\n" + // Include 3D view
  "                ]\n" +
  "            }\n" +
  "        ]\n" +
  "    }\n" +
  "}", null, "application/json"); // Specify content type as JSON

// Attach the content to the request
request.Content = content; // Add the request body to the HTTP request

// Send the request and await the response
var response = await client.SendAsync(request); // Send the POST request to the API

// Ensure the response indicates success
response.EnsureSuccessStatusCode(); // Throw an exception if the response status is not successful

// Output the response content
Console.WriteLine(await response.Content.ReadAsStringAsync()); // Print the response content to the console
```

```
import requests
import json

# Define the API endpoint URL
url = "https://developer.api.autodesk.com/modelderivative/v2/designdata/job"  # Endpoint for the Model Derivative API

# Define the request payload
payload = json.dumps({
  "input": {
   "urn": "dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6anAtYnVja2V0LTI1MDUwMi9ib3guaXB0"  # Unique identifier for the file to be translated
  },
  "output": {
   "destination": {
    "region": "EMEA"  # Specify the region where the output will be stored
   },
   "formats": [
    {
      "type": "svf2",  # Specify the output format as SVF2
      "views": [
       "2d",  # Include 2D view in the output
       "3d"   # Include 3D view in the output
      ]
    }
   ]
  }
})

# Define the request headers
headers = {
  'Content-Type': 'application/json',  # Specify the content type as JSON
  'Authorization': 'Bearer eyJh...',   # Add the authorization token for API access
}

# Send the POST request to the API
response = requests.request("POST", url, headers=headers, data=payload)  # Make the API call with the defined payload and headers

# Print the response from the API
print(response.text)  # Output the API response to the console
```

### Response

```
HTTP/1.1 201 Created
Server: nginx
Date: Fri, 02 May 2025 10:31:26 GMT
Content-Type: application/json; charset=utf-8
Content-Length: 261
Connection: keep-alive
x-ads-duration: 507 ms
x-ads-startup-time: Thu Apr 24 11:14:32 UTC 2025
x-ads-app-identifier: platform-viewing-2025.02.01.1205.2f1fdcae1-staging
strict-transport-security: max-age=31536000; includeSubDomains; preload
x-frame-options: SAMEORIGIN
x-request-id: fd05acb2-2641-47d3-97e6-3be4b6f09eeb

{
  "result": "created",
  "urn": "dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6anAtYnVja2V0LTI1MDUwMi9ib3guaXB0",
  "registerKeys": [
    "19bf3430-8f6e-472f-adfd-81532eff0157"
  ],
  "acceptedJobs": {
    "output": {
      "destination": {
        "region": "EMEA"
      },
      "formats": [
        {
          "type": "svf2",
          "views": [
            "2d",
            "3d"
          ]
        }
      ]
    }
  }
}
```

## Example 3

This example demonstrates a successful attempt at kicking off a job for the translation of an Inventor model to SVF2, where the source files are packaged as a single zip file.

The `compressedUrn` attribute is set to true, indicating that the source files are packaged as a single zip file. The main assembly file is specified by the `rootFilename` attribute.

### Request

```
curl --location 'https://developer.api.autodesk.com/modelderivative/v2/designdata/job' \
  --header 'Content-Type: application/json' \
  --header 'Authorization: Bearer eyJh...' \
  --data '{
    "input": {
      "urn": "dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6c3ZmMl8yMDI1XzA0XzE4X3VzL3R1bmVyLnppcA==",
      "rootFilename": "Tuner.iam",
      "compressedUrn": true
    },
    "output": {
      "destination": {
        "region": "US"
      },
      "formats": [
        {
          "type": "stl",
          "advanced": {
            "format": "binary",
            "exportColor": true,
            "exportFileStructure": "single"
          }
        }
      ]
    }
  }'
```

```
// Create headers for the request
const myHeaders = new Headers();
myHeaders.append("Content-Type", "application/json");
myHeaders.append("Authorization", "Bearer eyJh...");

// Define the request body with input and output parameters
const raw = JSON.stringify({
  "input": {
    "urn": "dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6c3ZmMl8yMDI1XzA0XzE4X3VzL3R1bmVyLnppcA==", // Unique identifier for the file
    "rootFilename": "Tuner.iam", // Main assembly file
    "compressedUrn": true // Indicates the source files are packaged as a single zip file
  },
  "output": {
    "destination": {
      "region": "US" // Specifies the region for the output
    },
    "formats": [
      {
        "type": "stl", // Output format
        "advanced": {
          "format": "binary", // Binary format for STL
          "exportColor": true, // Export with color
          "exportFileStructure": "single" // Single file structure
        }
      }
    ]
  }
});

// Configure the request options
const requestOptions = {
  method: "POST",
  headers: myHeaders,
  body: raw,
  redirect: "follow"
};

// Send the request using the Fetch API
fetch("https://developer.api.autodesk.com/modelderivative/v2/designdata/job", requestOptions)
  .then((response) => response.text()) // Parse the response as text
  .then((result) => console.log(result)) // Log the result
  .catch((error) => console.error(error)); // Handle any errors
```

```
// Create an HTTP client
var client = new HttpClient();

// Create a POST request to the Model Derivative API
var request = new HttpRequestMessage(HttpMethod.Post, "https://developer.api.autodesk.com/modelderivative/v2/designdata/job");

// Add required headers
request.Headers.Add("Authorization", "Bearer eyJh...");

// Define the request body with input and output parameters
var content = new StringContent(
  "{\n" +
  "    \"input\": {\n" +
  "        \"urn\": \"dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6c3ZmMl8yMDI1XzA0XzE4X3VzL3R1bmVyLnppcA==\",\n" +
  "        \"rootFilename\": \"Tuner.iam\",\n" +
  "        \"compressedUrn\": true\n" +
  "    },\n" +
  "    \"output\": {\n" +
  "        \"destination\": {\n" +
  "            \"region\": \"US\"\n" +
  "        },\n" +
  "        \"formats\": [\n" +
  "            {\n" +
  "                \"type\": \"stl\",\n" +
  "                \"advanced\": {\n" +
  "                    \"format\": \"binary\",\n" +
  "                    \"exportColor\": true,\n" +
  "                    \"exportFileStructure\": \"single\"\n" +
  "                }\n" +
  "            }\n" +
  "        ]\n" +
  "    }\n" +
  "}", null, "application/json");

// Attach the content to the request
request.Content = content;

// Send the request and await the response
var response = await client.SendAsync(request);

// Ensure the response indicates success
response.EnsureSuccessStatusCode();

// Output the response content
Console.WriteLine(await response.Content.ReadAsStringAsync());
```

```
# Import required libraries
import requests
import json

# Define the API endpoint URL
url = "https://developer.api.autodesk.com/modelderivative/v2/designdata/job"

# Define the request payload with input and output parameters
payload = json.dumps({
  "input": {
    "urn": "dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6c3ZmMl8yMDI1XzA0XzE4X3VzL3R1bmVyLnppcA==",  # Unique identifier for the file
    "rootFilename": "Tuner.iam",  # Main assembly file
    "compressedUrn": True  # Indicates the source files are packaged as a single zip file
  },
  "output": {
    "destination": {
      "region": "US"  # Specifies the region for the output
    },
    "formats": [
      {
        "type": "stl",  # Output format
        "advanced": {
          "format": "binary",  # Binary format for STL
          "exportColor": True,  # Export with color
          "exportFileStructure": "single"  # Single file structure
        }
      }
    ]
  }
})

# Define the request headers
headers = {
  'Content-Type': 'application/json',
  'Authorization': 'Bearer eyJh...'  # Replace with a valid token
}

# Send the POST request to the API
response = requests.request("POST", url, headers=headers, data=payload)

# Print the response from the API
print(response.text)
```

### Response

```
HTTP/1.1 200 OK
Server: nginx
Date: Mon, 28 Apr 2025 10:12:04 GMT
Content-Type: application/json; charset=utf-8
Transfer-Encoding: chunked
Connection: keep-alive
vary: Accept-Encoding
x-ads-duration: 102 ms
content-encoding: gzip
x-ads-startup-time: Thu Apr 24 11:14:32 UTC 2025
x-ads-app-identifier: platform-viewing-2025.02.01.1205.2f1fdcae1-staging
strict-transport-security: max-age=31536000; includeSubDomains; preload
x-frame-options: SAMEORIGIN
x-request-id: 3b745aba-2d86-47ba-8b05-b41e5f3d8067

 {
    "result": "success",
    "urn": "dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6c3ZmMl8yMDI1XzA0XzE4X3VzL3R1bmVyLnppcA",
    "acceptedJobs": {
       "output": {
          "destination": {
             "region": "US"
          },
          "formats": [
             {
                "type": "stl",
                "advanced": {
                   "format": "binary",
                   "exportColor": true,
                   "exportFileStructure": "single"
                }
             }
          ]
       }
    }
 }
```

## Example 4

This example demonstrates a successful attempt at starting a job to export geometry corresponding to the specified object ids to the OBJ format (200).

### Request

```
curl --location 'https://developer.api.autodesk.com/modelderivative/v2/designdata/job' \
  --header 'Content-Type: application/json' \
  --header 'Authorization: Bearer eyJh...' \
  --data '{
      "input": {
        "urn": "dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6anAtYnVja2V0LTI1MDUwMi9zdGFwbGVyLnppcA=="
      },
      "output": {
        "destination": {
          "region": "us"
        },
        "formats": [
          {
            "type": "obj",
            "advanced": {
              "modelGuid": "7b2b4bb0-2285-4cca-b525-ea0e85c05f00",
              "objectIds": [4, 5]
            }
          }
        ]
      }
  }'
```

```
// Create headers for the request
const myHeaders = new Headers();
myHeaders.append("Content-Type", "application/json"); // Specify the content type
myHeaders.append("Authorization", "Bearer eyJh..."); // Add authorization token

// Define the request payload
const raw = JSON.stringify({
  input: {
    urn: "dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6anAtYnVja2V0LTI1MDUwMi9zdGFwbGVyLnppcA==" // Unique identifier for the file
  },
  output: {
    destination: {
      region: "us" // Specify the region for the output
    },
    formats: [
      {
        type: "obj", // Output format
        advanced: {
          modelGuid: "7b2b4bb0-2285-4cca-b525-ea0e85c05f00", // Model GUID
          objectIds: [4, 5] // Object IDs to extract
        }
      }
    ]
  }
});

// Configure the request options
const requestOptions = {
  method: "POST", // HTTP method
  headers: myHeaders, // Attach headers
  body: raw, // Attach the request payload
  redirect: "follow" // Follow redirects if any
};

// Send the request using the Fetch API
fetch("https://developer.api.autodesk.com/modelderivative/v2/designdata/job", requestOptions)
  .then((response) => response.text()) // Parse the response as text
  .then((result) => console.log(result)) // Log the result
  .catch((error) => console.error(error)); // Handle any errors
```

```
// Create an HTTP client
var client = new HttpClient();

// Create a POST request to the Model Derivative API
var request = new HttpRequestMessage(HttpMethod.Post, "https://developer.api.autodesk.com/modelderivative/v2/designdata/job");

// Add required headers
request.Headers.Add("Authorization", "Bearer eyJh...");

// Define the request body with input and output parameters
var content = new StringContent(
  "{\n" +
  "    \"input\": {\n" +
  "        \"urn\": \"dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6anAtYnVja2V0LTI1MDUwMi9zdGFwbGVyLnppcA==\"\n" + // Unique identifier for the file
  "    },\n" +
  "    \"output\": {\n" +
  "        \"destination\": {\n" +
  "            \"region\": \"us\"\n" + // Specify the region for the output
  "        },\n" +
  "        \"formats\": [\n" +
  "            {\n" +
  "                \"type\": \"obj\",\n" + // Output format
  "                \"advanced\": {\n" +
  "                    \"modelGuid\": \"7b2b4bb0-2285-4cca-b525-ea0e85c05f00\",\n" + // Model GUID
  "                    \"objectIds\": [4, 5]\n" + // Object IDs to extract
  "                }\n" +
  "            }\n" +
  "        ]\n" +
  "    }\n" +
  "}", null, "application/json");

// Attach the content to the request
request.Content = content;

// Send the request and await the response
var response = await client.SendAsync(request);

// Ensure the response indicates success
response.EnsureSuccessStatusCode();

// Output the response content
Console.WriteLine(await response.Content.ReadAsStringAsync());
```

```
import requests
import json

# Define the API endpoint URL
url = "https://developer.api.autodesk.com/modelderivative/v2/designdata/job"  # Endpoint for the Model Derivative API

# Define the request payload
payload = json.dumps({
  "input": {
    "urn": "dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6anAtYnVja2V0LTI1MDUwMi9zdGFwbGVyLnppcA=="  # Unique identifier for the file
  },
  "output": {
    "destination": {
      "region": "us"  # Specify the region for the output
    },
    "formats": [
      {
        "type": "obj",  # Output format
        "advanced": {
          "modelGuid": "7b2b4bb0-2285-4cca-b525-ea0e85c05f00",  # Model GUID
          "objectIds": [
            4,  # Object ID to extract
            5   # Object ID to extract
          ]
        }
      }
    ]
  }
})

# Define the request headers
headers = {
  'Content-Type': 'application/json',  # Specify the content type as JSON
  'Authorization': 'Bearer eyJh...'    # Add the authorization token for API access
}

# Send the POST request to the API
response = requests.request("POST", url, headers=headers, data=payload)  # Make the API call with the defined payload and headers

# Print the response from the API
print(response.text)  # Output the API response to the console
```

### Response

```
HTTP/1.1 200 OK
Server: nginx
Date: Fri, 02 May 2025 11:34:12 GMT
Content-Type: application/json; charset=utf-8
Content-Length: 270
Connection: keep-alive
x-ads-duration: 490 ms
x-ads-startup-time: Thu Apr 24 11:14:31 UTC 2025
x-ads-app-identifier: platform-viewing-2025.02.01.1205.2f1fdcae1-staging
strict-transport-security: max-age=31536000; includeSubDomains; preload
x-frame-options: SAMEORIGIN
x-request-id: fa9ed39a-621d-4079-9e47-e9bfad894948

{
  "result": "success",
  "urn": "dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6anAtYnVja2V0LTI1MDUwMi9zdGFwbGVyLnppcA",
  "acceptedJobs": {
    "output": {
      "destination": {
        "region": "us"
      },
      "formats": [
        {
          "type": "obj",
          "advanced": {
            "modelGuid": "7b2b4bb0-2285-4cca-b525-ea0e85c05f00",
            "objectIds": [
              4,
              5
            ]
          }
        }
      ]
    }
  }
}
```

## Example 5

This example demonstrates a failed attempt to start a job for extracting geometry. The job request failed because the specified object ids are invalid (400).

### Request

```
curl --location 'https://developer.api.autodesk.com/modelderivative/v2/designdata/job' \
    --header 'Content-Type: application/json' \
    --header 'Authorization: Bearer eyJh...' \
    --data '{
      "input": {
        "urn": "dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6anAtYnVja2V0LTI1MDUwMi9zdGFwbGVyLnppcA=="
      },
      "output": {
        "destination": {
          "region": "us"
        },
        "formats": [
          {
            "type": "obj",
            "advanced": {
              "modelGuid": "7b2b4bb0-2285-4cca-b525-ea0e85c05f00",
              "objectIds": [-2]
            }
          }
        ]
      }
    }'
```

```
// Create headers for the request
const myHeaders = new Headers();
myHeaders.append("Content-Type", "application/json"); // Specify the content type as JSON
myHeaders.append("Authorization", "Bearer eyJh..."); // Add the authorization token

// Define the request payload
const raw = JSON.stringify({
  input: {
    urn: "dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6anAtYnVja2V0LTI1MDUwMi9zdGFwbGVyLnppcA==", // Unique identifier for the file
  },
  output: {
    destination: {
      region: "us", // Specify the region for the output
    },
    formats: [
      {
        type: "obj", // Output format
        advanced: {
          modelGuid: "7b2b4bb0-2285-4cca-b525-ea0e85c05f00", // Model GUID
          objectIds: [-2], // Invalid object ID (should be >= 0 or -1 for the whole model)
        },
      },
    ],
  },
});

// Configure the request options
const requestOptions = {
  method: "POST", // HTTP method
  headers: myHeaders, // Attach headers
  body: raw, // Attach the request payload
  redirect: "follow", // Follow redirects if any
};

// Send the request using the Fetch API
fetch("https://developer.api.autodesk.com/modelderivative/v2/designdata/job", requestOptions)
  .then((response) => response.text()) // Parse the response as text
  .then((result) => console.log(result)) // Log the result
  .catch((error) => console.error(error)); // Handle any errors
```

```
// Create an HTTP client
var client = new HttpClient();

// Create a POST request to the Model Derivative API
var request = new HttpRequestMessage(HttpMethod.Post, "https://developer.api.autodesk.com/modelderivative/v2/designdata/job");

// Add required headers
request.Headers.Add("Authorization", "Bearer eyJh..."); // Authorization token for the API

// Define the request body with input and output parameters
var content = new StringContent(
  "{\n" +
  "    \"input\": {\n" +
  "        \"urn\": \"dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6anAtYnVja2V0LTI1MDUwMi9zdGFwbGVyLnppcA==\"\n" + // Unique identifier for the file
  "    },\n" +
  "    \"output\": {\n" +
  "        \"destination\": {\n" +
  "            \"region\": \"us\"\n" + // Specify the region for the output
  "        },\n" +
  "        \"formats\": [\n" +
  "            {\n" +
  "                \"type\": \"obj\",\n" + // Output format
  "                \"advanced\": {\n" +
  "                    \"modelGuid\": \"7b2b4bb0-2285-4cca-b525-ea0e85c05f00\",\n" + // Model GUID
  "                    \"objectIds\": [-2]\n" + // Invalid object ID (should be >= 0 or -1 for the whole model)
  "                }\n" +
  "            }\n" +
  "        ]\n" +
  "    }\n" +
  "}", null, "application/json"); // Specify content type as JSON

// Attach the content to the request
request.Content = content;

// Send the request and await the response
var response = await client.SendAsync(request);

// Ensure the response indicates success
response.EnsureSuccessStatusCode(); // Throw an exception if the response status is not successful

// Output the response content
Console.WriteLine(await response.Content.ReadAsStringAsync()); // Print the response content to the console
```

```
import requests
import json

# Define the API endpoint URL
url = "https://developer.api.autodesk.com/modelderivative/v2/designdata/job"

# Define the request payload with input and output parameters
payload = json.dumps({
  "input": {
    "urn": "dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6anAtYnVja2V0LTI1MDUwMi9zdGFwbGVyLnppcA=="  # Unique identifier for the file
  },
  "output": {
    "destination": {
      "region": "us"  # Specify the region for the output
    },
    "formats": [
      {
        "type": "obj",  # Output format
        "advanced": {
          "modelGuid": "7b2b4bb0-2285-4cca-b525-ea0e85c05f00",  # Model GUID
          "objectIds": [
            -2  # Invalid object ID (should be >= 0 or -1 for the whole model)
          ]
        }
      }
    ]
  }
})

# Define the request headers
headers = {
  'Content-Type': 'application/json',  # Specify the content type as JSON
  'Authorization': 'Bearer eyJh...'  # Add the authorization token for API access
}

# Send the POST request to the API
response = requests.request("POST", url, headers=headers, data=payload)

# Print the response from the API
print(response.text)  # Output the API response to the console
```

### Response

```
HTTP/1.1 400 Bad Request
Server: nginx
Date: Fri, 02 May 2025 14:29:14 GMT
Content-Type: application/json; charset=utf-8
Content-Length: 140
Connection: keep-alive
x-ads-duration: 0 ms
x-ads-exception-id: fdad1836-0ff2-42cc-b6b7-d36a43f4bfbf
x-ads-startup-time: Thu Apr 24 11:14:31 UTC 2025
x-ads-app-identifier: platform-viewing-2025.02.01.1205.2f1fdcae1-staging
x-ads-troubleshooting: The request is invalid.
strict-transport-security: max-age=31536000; includeSubDomains; preload
x-frame-options: SAMEORIGIN
x-request-id: b3b34d8e-588d-403c-bc26-856b4cef420d

{
  "diagnostic": "The values of 'objectIds' should be equal or greater than 0. You can also set the value as [-1] for getting the whole mode."
}
```

## Example 6

Successful creation of a job with webhook workflow specified (200)

### Request

```
curl --location 'https://developer.api.autodesk.com/modelderivative/v2/designdata/job' \
  --header 'Content-Type: application/json' \
  --header 'Authorization: Bearer eyJh...-Nt4eKjFqQ79Afwe7pqpbu9bT2ZAjxVP56Tcz7YB0kClQhsxDR3sk4vaB22I9IImkPemNdaPQ6XHJtk1Dj2up9qFWteWTxYiV7WMlc5sTsg' \
  --data '{
    "input": {
      "urn": "dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6anAtYnVja2V0LTI1MDUwMi90dW5lci56aXA=",
      "rootFilename": "Tuner.iam",
      "compressedUrn": true
    },
    "misc": {
      "workflow": "my-workflow-id",
      "workflowAttribute": {
        "myfoo": 33,
        "projectId": "someURN",
        "myobject": {
          "nested": true
        }
      }
    },
    "output": {
      "destination": {
        "region": "us"
      },
      "formats": [
        {
          "type": "stl",
          "advanced": {
            "format": "binary",
            "exportColor": true,
            "exportFileStructure": "single"
          }
        }
      ]
    }
  }'
```

```
const myHeaders = new Headers();
myHeaders.append("Content-Type", "application/json");
myHeaders.append("Authorization", "Bearer eyJh...-Nt4eKjFqQ79Afwe7pqpbu9bT2ZAjxVP56Tcz7YB0kClQhsxDR3sk4vaB22I9IImkPemNdaPQ6XHJtk1Dj2up9qFWteWTxYiV7WMlc5sTsg");

const raw = JSON.stringify({
  "input": {
    "urn": "dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6anAtYnVja2V0LTI1MDUwMi90dW5lci56aXA=",
    "rootFilename": "Tuner.iam",
    "compressedUrn": true
  },
  "misc": {
    "workflow": "my-workflow-id",
    "workflowAttribute": {
      "myfoo": 33,
      "projectId": "someURN",
      "myobject": {
        "nested": true
      }
    }
  },
  "output": {
    "destination": {
      "region": "us"
    },
    "formats": [
      {
        "type": "stl",
        "advanced": {
          "format": "binary",
          "exportColor": true,
          "exportFileStructure": "single"
        }
      }
    ]
  }
});

const requestOptions = {
  method: "POST",
  headers: myHeaders,
  body: raw,
  redirect: "follow"
};

fetch("https://developer.api.autodesk.com/modelderivative/v2/designdata/job", requestOptions)
  .then((response) => response.text())
  .then((result) => console.log(result))
  .catch((error) => console.error(error));
```

```
var client = new HttpClient();

// Create a POST request to the Model Derivative API
var request = new HttpRequestMessage(HttpMethod.Post, "https://developer.api.autodesk.com/modelderivative/v2/designdata/job");

// Add required headers
request.Headers.Add("Authorization", "Bearer eyJh...-Nt4eKjFqQ79Afwe7pqpbu9bT2ZAjxVP56Tcz7YB0kClQhsxDR3sk4vaB22I9IImkPemNdaPQ6XHJtk1Dj2up9qFWteWTxYiV7WMlc5sTsg");

// Define the request body with input, misc, and output parameters
var content = new StringContent(
  "{\n" +
  "    \"input\": {\n" +
  "        \"urn\": \"dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6anAtYnVja2V0LTI1MDUwMi90dW5lci56aXA=\",\n" +
  "        \"rootFilename\": \"Tuner.iam\",\n" +
  "        \"compressedUrn\": true\n" +
  "    },\n" +
  "    \"misc\": {\n" +
  "        \"workflow\": \"my-workflow-id\",\n" +
  "        \"workflowAttribute\": {\n" +
  "            \"myfoo\": 33,\n" +
  "            \"projectId\": \"someURN\",\n" +
  "            \"myobject\": {\n" +
  "                \"nested\": true\n" +
  "            }\n" +
  "        }\n" +
  "    },\n" +
  "    \"output\": {\n" +
  "        \"destination\": {\n" +
  "            \"region\": \"us\"\n" +
  "        },\n" +
  "        \"formats\": [\n" +
  "            {\n" +
  "                \"type\": \"stl\",\n" +
  "                \"advanced\": {\n" +
  "                    \"format\": \"binary\",\n" +
  "                    \"exportColor\": true,\n" +
  "                    \"exportFileStructure\": \"single\"\n" +
  "                }\n" +
  "            }\n" +
  "        ]\n" +
  "    }\n" +
  "}", null, "application/json");

// Attach the content to the request
request.Content = content;

// Send the request and await the response
var response = await client.SendAsync(request);

// Ensure the response indicates success
response.EnsureSuccessStatusCode();

// Output the response content
Console.WriteLine(await response.Content.ReadAsStringAsync());
```

```
import requests
import json

# Define the API endpoint URL
url = "https://developer.api.autodesk.com/modelderivative/v2/designdata/job"

# Define the request payload
payload = json.dumps({
  "input": {
    "urn": "dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6anAtYnVja2V0LTI1MDUwMi90dW5lci56aXA=",  # Unique identifier for the file
    "rootFilename": "Tuner.iam",  # Main assembly file
    "compressedUrn": True  # Indicates the source files are packaged as a single zip file
  },
  "misc": {
    "workflow": "my-workflow-id",  # Workflow ID for the job
    "workflowAttribute": {
      "myfoo": 33,  # Custom attribute for the workflow
      "projectId": "someURN",  # Project identifier
      "myobject": {
        "nested": True  # Example of a nested object
      }
    }
  },
  "output": {
    "destination": {
      "region": "us"  # Specifies the region for the output
    },
    "formats": [
      {
        "type": "stl",  # Output format
        "advanced": {
          "format": "binary",  # Binary format for STL
          "exportColor": True,  # Export with color
          "exportFileStructure": "single"  # Single file structure
        }
      }
    ]
  }
})

# Define the request headers
headers = {
  'Content-Type': 'application/json',  # Specify the content type as JSON
  'Authorization': 'Bearer eyJh...-Nt4eKjFqQ79Afwe7pqpbu9bT2ZAjxVP56Tcz7YB0kClQhsxDR3sk4vaB22I9IImkPemNdaPQ6XHJtk1Dj2up9qFWteWTxYiV7WMlc5sTsg'  # Add the authorization token
}

# Send the POST request to the API
response = requests.request("POST", url, headers=headers, data=payload)

# Print the response from the API
print(response.text)
```

### Response

```
HTTP/1.1 200 OK
Server: nginx
Date: Fri, 02 May 2025 14:44:12 GMT
Content-Type: application/json; charset=utf-8
Content-Length: 266
Connection: keep-alive
x-ads-duration: 399 ms
x-ads-startup-time: Thu Apr 24 11:14:31 UTC 2025
x-ads-app-identifier: platform-viewing-2025.02.01.1205.2f1fdcae1-staging
strict-transport-security: max-age=31536000; includeSubDomains; preload
x-frame-options: SAMEORIGIN
x-request-id: 88bc24ff-2e3b-4b17-933f-a9fa07f1c96c

{
  "result": "success",
  "urn": "dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6anAtYnVja2V0LTI1MDUwMi90dW5lci56aXA",
  "acceptedJobs": {
    "output": {
      "destination": {
        "region": "us"
      },
      "formats": [
        {
          "type": "stl",
          "advanced": {
            "format": "binary",
            "exportColor": true,
            "exportFileStructure": "single"
          }
        }
      ]
    }
  }
}
```

---
원본 문서: https://aps.autodesk.com/en/docs/model-derivative/v2/reference/http/job-POST
